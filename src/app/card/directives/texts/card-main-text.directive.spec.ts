import { CardMainTextDirective } from './card-main-text.directive';

describe('CardMainTextDirective', () => {
  it('should create an instance', () => {
    const directive = new CardMainTextDirective();
    expect(directive).toBeTruthy();
  });
});
